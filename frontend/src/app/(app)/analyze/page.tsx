import { Placeholder } from "@/components/Placeholder";

export default function AnalyzePage() {
  return (
    <Placeholder
      title="Analyze"
      note="Pick a saved résumé, upload, or paste — then add a job description to get an AI match report."
    />
  );
}
