# ---- Phase 1: API + data connectivity ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
redis==5.2.1

# ---- Phase 2: database migrations ----
alembic==1.14.0

# ---- Phase 3: auth (JWT + password hashing) ----
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
email-validator==2.2.0

# ---- Phase 4: AI (LangChain; providers: OpenRouter/Gemini/DeepSeek) ----
langchain-google-genai>=2.0,<3
langchain-openai>=0.3,<2

# ---- Phase 5: RAG (PDF -> chunk -> local embeddings -> ChromaDB) ----
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
sentence-transformers>=3,<4
chromadb>=0.5,<0.7
pypdf>=5,<6

# ---- Phase 6: object storage (S3/MinIO) + more resume formats ----
boto3>=1.35,<2
python-docx>=1.1,<2
